# pytest.ini  – nur das hier, ohne Kommentar in derselben Zeile
[pytest]
DJANGO_SETTINGS_MODULE = core.test_settings
python_files = tests.py test_*.py *_tests.py
addopts = --reuse-db --nomigrations
